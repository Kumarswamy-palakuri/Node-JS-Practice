<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <title>Blog List</title>
</head>
<body>
    <header>
        <h1>Blog Posts</h1>
       <button> <a href="/blogs/new">Create new Blog</a></button>
    </header>
    <section>
        <% blogs.forEach(blog => { %>
            <div class="blog-post">
                <h2><%= blog.title %></h2>
                <p>by <strong><%= blog.author %></strong></p>  <!-- Display author name -->
                <p><%= blog.content.substring(0, 150) %>...</p>
                <small>Posted on: <%= blog.date.toDateString() %></small>
            </div>
        <% }) %>
    </section>
</body>
</html>
